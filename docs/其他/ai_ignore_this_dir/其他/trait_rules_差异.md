# trait_rules.md → trait_rules_v1.md 更新差异

Version: 1.0 → 1.1

---

## 变更总览

| 变更类型 | 数量 |
|----------|------|
| 新增 | 5 |
| 修改 | 10 |
| 删除 | 0 |

---

## 逐项变更

### 1. 核心原则（修改）

**位置**：核心原则

**变更类型**：修改

**原内容**：
```
- Trait 表示能力，不表示分类
- 组合优于继承
- 统一扩展机制
- Handler 分发
```

**新内容**：
```
- 🟥 Trait 表示能力，不表示分类
- 🟥 组合优于继承（宪法 1.1.10）
- 🟥 统一扩展机制（宪法 1.1.6）
- 🟥 Handler 分发（宪法 6.0.2）
```

**原因**：宪法要求所有规则标注强制等级；补充宪法条款引用。Trait 领域是宪法 1.1.6 和 1.1.10 的核心体现。

---

### 2. 不变量1-4 添加强制等级（修改）

**位置**：不变量1-4

**变更类型**：修改

**原内容**：无强制等级标记

**新内容**：
```
不变量1：Passive 效果仅 GrantTag 和 ModifyAttribute 🟥
不变量2：触发型效果仅 ApplyBuff 🟥
不变量3：Handler 覆盖所有效果类型 🟥
不变量4：修饰符 Source 区间隔离 🟥
```

**原因**：宪法要求所有规则标注强制等级。所有不变量均为🟥绝对禁止级别。

---

### 3. 不变量5：修饰符必须通过 Modifier 管线（新增）

**位置**：不变量

**变更类型**：新增

**新内容**：
```
## 不变量5：修饰符必须通过 Modifier 管线 🟥

宪法依据：2.2.1（禁止直接修改最终属性值）

Trait 的 ModifyAttribute 效果完成后：
属性修饰必须通过 ModifierSource::trait_source 添加到 Attributes，禁止直接修改属性值。

违反表现：
Trait 直接修改 HP、ATK 等最终属性值。

架构违规检测：
ARCHITECTURE VIOLATION: Trait 直接修改属性值，违反"属性修改必须通过 Modifier 管线"原则。
```

**原因**：宪法 2.2.1 明确禁止直接修改最终属性值，Trait 修饰也必须遵守。

---

### 4. 规则1-4 添加强制等级和宪法引用（修改）

**位置**：业务规则1-4

**变更类型**：修改

**原内容**：无强制等级标记，无宪法引用

**新内容**：
```
规则1：效果与触发器对应 🟥
规则2：Handler 分发 🟥
宪法依据：6.0.2（Trait 用于扩展点）
规则3：来源追踪 🟥
规则4：Trait 重建 🟥
```

**原因**：宪法要求所有规则标注强制等级和宪法条款引用。

---

### 5. 规则1-4 禁止事项添加强制等级（修改）

**位置**：规则1-4

**变更类型**：修改

**原内容**：无强制等级标记

**新内容**：
```
规则1禁止：
- 🟥 Passive Trait 使用 ApplyBuff 效果
- 🟥 触发型 Trait 使用 GrantTag / ModifyAttribute 效果

规则2禁止：
- 🟥 match 分发效果类型
- 🟥 为每种效果类型修改 TraitData 方法

规则3禁止：
- 🟥 不区分来源直接增删 Trait
- 🟥 装备穿脱时误删内在 Trait

规则4禁止：
- 🟥 穿脱装备后跳过 Trait 重建
- 🟥 增量更新 Trait 效果
```

**原因**：宪法要求所有禁止事项标注强制等级。

---

### 6. 流程管线步骤添加强制等级（修改）

**位置**：流程管线 → 被动 Trait 应用管线 Step1-3 / Trait 重建管线 Step1-3

**变更类型**：修改

**原内容**：无强制等级标记

**新内容**：
```
被动 Trait 应用管线：
Step1: 🟥 禁止：处理非 Passive 触发
Step2: 🟥 禁止：跳过任何效果
Step3: 🟥 禁止：Source 区间与其他来源冲突

Trait 重建管线：
Step1: 🟥 禁止：清除 from_equipment
Step2: 🟥 禁止：跳过任何 Passive Trait
Step3: 🟥 禁止：包含 Buff 层标签
```

**原因**：宪法要求所有禁止事项标注强制等级。

---

### 7. 数据结构添加强制等级（修改）

**位置**：数据结构 → TraitData / TraitEffect / TraitEffectHandler / TraitCollection / TraitSource

**变更类型**：修改

**新增内容**：
```
TraitData: 🟥 RON 配置路径（宪法 1.1.5）
TraitEffect: 🟥 与 TraitTrigger 严格对应
TraitEffectHandler: 🟥 新增效果类型只需实现并注册（宪法 6.0.2）
TraitCollection: 🟥 remove_by_source 精确清理
TraitSource: 🟥 装备穿脱时使用 Equipment 变体、🟥 内在 Trait 使用 Intrinsic 变体
```

**原因**：宪法要求所有规则标注强制等级和宪法条款引用。

---

### 8. 禁止事项添加强制等级和宪法引用（修改）

**位置**：禁止事项

**变更类型**：修改

**原内容**：无强制等级标记，无宪法引用

**新内容**：5条禁止事项全部添加🟥标记和宪法引用

```
🟥 禁止：为每种能力来源写独立逻辑（宪法 1.1.6）
🟥 禁止：match 分发效果类型（宪法 6.0.2）
🟥 禁止：Passive Trait 使用 ApplyBuff 效果
🟥 禁止：穿脱装备后跳过 Trait 重建
🟥 禁止：增量更新 Trait 效果
```

**原因**：宪法要求所有禁止事项标注强制等级和宪法条款引用。

---

### 9. 禁止事项新增1条（新增）

**位置**：禁止事项

**变更类型**：新增

**新内容**：
```
🟥 禁止：Trait 直接修改属性值
原因：宪法 2.2.1 禁止直接修改最终属性值
架构违规检测：ARCHITECTURE VIOLATION: Trait 直接修改属性值...
```

**原因**：宪法 2.2.1 明确要求。

---

### 10. 禁止事项新增架构违规检测（修改）

**位置**：禁止事项 → "match 分发效果类型"

**变更类型**：修改

**新增内容**：
```
架构违规检测：
ARCHITECTURE VIOLATION: match 分发效果类型，违反"Handler 通过 type_name 分发"原则。
```

**原因**：match 分发是核心架构违规。

---

### 11. AI 修改规则添加强制等级和测试要求（修改）

**位置**：AI 修改规则

**变更类型**：修改

**新增内容**：
```
禁止：
- 🟥 修改 TraitData 方法
- 🟥 修改 apply_passive_traits 流程
- 🟥 修改 rebuild_trait_effects 流程
- 🟥 修改现有触发器的效果对应关系
- 🟥 修改 Handler 分发逻辑
- 🟥 修改 TraitData 结构
- 🟥 修改 TraitRegistry 加载流程

测试要求（宪法 13.0.1-13.0.3）：
- 🟩 单元测试：验证 Handler 分发和效果收集
- 🟩 集成测试：验证 Trait 重建流程
- 🟩 Bug 修复必须先编写重现测试（宪法 13.0.2）
```

**原因**：宪法要求所有禁止事项标注强制等级；宪法 13.0.1-13.0.3 要求测试优先。

---

### 12. 宪法条款映射（新增）

**位置**：文档末尾

**变更类型**：新增

**原因**：便于 AI 快速定位宪法条款与领域规则的对应关系。

---

### 13. 架构违规检测（新增）

**位置**：文档末尾

**变更类型**：新增

**原因**：架构文档定义了标准违规检测模式，领域文档应纳入。

---

## 变更统计

| 强制等级 | 原文档数量 | 新文档数量 |
|----------|-----------|-----------|
| 🟥 绝对禁止 | 0 | 16 |
| 🟩 必须遵守 | 0 | 6 |
| 宪法条款引用 | 0 | 5 |
| 架构违规检测 | 0 | 2 |
