# inventory_rules.md → inventory_rules_v1.md 更新差异

Version: 1.0 → 1.1

---

## 变更总览

| 变更类型 | 数量 |
|----------|------|
| 新增 | 7 |
| 修改 | 10 |
| 删除 | 0 |

---

## 逐项变更

### 1. 核心原则（修改）

**位置**：核心原则

**变更类型**：修改

**原内容**：
```
- 统一容器模型
- Definition / Instance 分离
- 堆叠合并
- 容量与重量双限制
```

**新内容**：
```
- 🟩 统一容器模型
- 🟥 Definition / Instance 分离（宪法 1.1.2）
- 🟩 堆叠合并
- 🟩 容量与重量双限制
- 🟥 Rule / Content 分离（宪法 1.1.3）
```

**原因**：宪法要求所有规则标注强制等级；补充宪法条款引用；新增 Rule/Content 分离原则。

---

### 2. 跨领域通信方式修改（修改）

**位置**：领域边界 → 跨领域通信方式

**变更类型**：修改

**原内容**：
```
| 恢复属性 | 直接修改 Attributes | stat_system |
| 施加 Buff | 直接调用 apply_buff | buff |
```

**新内容**：
```
| 恢复属性 | 通过 ModifierSource 修改 Attributes | stat_system |
| 施加 Buff | 通过 apply_buff 接口 | buff |
```

**原因**：宪法 2.2.1 禁止直接修改属性值，修饰符必须通过 ModifierSource 添加。

---

### 3. 不变量1-5 添加强制等级（修改）

**位置**：不变量1-5

**变更类型**：修改

**原内容**：无强制等级标记

**新内容**：
```
不变量1：InstanceId 全局唯一 🟥
不变量2：堆叠数量不超过上限 🟥
不变量3：容器容量和重量约束 🟩
不变量4：空堆叠自动清理 🟥
不变量5：消耗品仅 Consumable 可使用 🟥
```

**原因**：宪法要求所有规则标注强制等级。

---

### 4. 不变量6：属性修改必须通过 Modifier 管线（新增）

**位置**：不变量

**变更类型**：新增

**新内容**：
```
## 不变量6：属性修改必须通过 Modifier 管线 🟥

宪法依据：2.2.1（禁止直接修改最终属性值）

消耗品使用完成后：
RestoreVital 效果必须通过 Modifier 管线修改属性，禁止直接修改最终属性值。

违反表现：
消耗品直接修改 HP、MP 等最终属性值。
```

**原因**：宪法 2.2.1 明确禁止直接修改最终属性值，消耗品恢复属性也必须遵守。

---

### 5. 规则1-5 添加强制等级（修改）

**位置**：业务规则1-5

**变更类型**：修改

**原内容**：无强制等级标记

**新内容**：
```
规则1：堆叠合并 🟩
规则2：容器操作 🟩
规则3：消耗品使用 🟥
规则4：容器间转移 🟩
规则5：战场背包 🟥
```

**原因**：宪法要求所有规则标注强制等级。消耗品使用和战场背包涉及数据一致性为🟥级别。

---

### 6. 规则3 禁止事项添加强制等级（修改）

**位置**：规则3

**变更类型**：修改

**原内容**：
```
禁止：
- 非 Consumable 类型使用 UseItem
```

**新内容**：
```
禁止：
- 🟥 非 Consumable 类型使用 UseItem
```

**原因**：宪法要求所有禁止事项标注强制等级。

---

### 7. 规则5 禁止事项添加强制等级（修改）

**位置**：规则5

**变更类型**：修改

**原内容**：
```
禁止：
- 战斗中直接操作角色背包
```

**新内容**：
```
禁止：
- 🟥 战斗中直接操作角色背包
```

**原因**：宪法要求所有禁止事项标注强制等级。

---

### 8. 流程管线步骤添加强制等级（修改）

**位置**：流程管线 → 添加物品管线 Step1-4 / 消耗品使用管线 Step1-4

**变更类型**：修改

**原内容**：无强制等级标记

**新内容**：
```
添加物品管线：
Step2: 🟥 禁止：跳过合并条件检查
Step3: 🟥 禁止：绕过容量/重量检查
Step4: 🟥 禁止：添加超过容量的物品

消耗品使用管线：
Step2: 🟥 禁止：非 Consumable 继续使用
Step3: 🟥 禁止：跳过任何效果
Step4: 🟥 禁止：归零时不清理
```

**原因**：宪法要求所有禁止事项标注强制等级。

---

### 9. 数据结构添加强制等级（修改）

**位置**：数据结构 → ItemDef / ItemInstance / ItemStack / Container / Resources

**变更类型**：修改

**新增内容**：
```
ItemDef: 🟥 不可变，加载后不修改（宪法 1.1.2）、🟥 RON 配置路径（宪法 1.1.5）
ItemInstance: 🟥 instance_id 全局唯一
ItemStack: 🟥 count ≤ def.stack_size
Container: 🟥 reduce_stack 归零自动清理
```

**原因**：宪法要求所有规则标注强制等级和宪法条款引用。

---

### 10. 禁止事项添加强制等级和新增2条（修改+新增）

**位置**：禁止事项

**变更类型**：修改 + 新增

**修改内容**：原有5条禁止事项全部添加🟥标记

**新增内容**：
```
🟥 禁止：直接修改最终属性值
原因：宪法 2.2.1 禁止直接修改最终属性值
架构违规检测：ARCHITECTURE VIOLATION: 消耗品直接修改属性值...

🟥 禁止：运行时修改 ItemDef
原因：宪法 1.1.2 Definition 不可变
```

**原因**：宪法 2.2.1 和 1.1.2 明确要求。

---

### 11. AI 修改规则添加强制等级和测试要求（修改）

**位置**：AI 修改规则

**变更类型**：修改

**新增内容**：
```
禁止：
- 🟥 修改 Container 核心操作
- 🟥 修改堆叠合并条件
- 🟥 修改容量/重量检查逻辑
- 🟥 修改现有 UseEffect 的逻辑
- 🟥 修改 UseItem Message 结构

测试要求（宪法 13.0.1-13.0.3）：
- 🟩 单元测试：验证堆叠合并/容器操作
- 🟩 集成测试：验证完整物品生命周期
- 🟩 Bug 修复必须先编写重现测试（宪法 13.0.2）
```

**原因**：宪法要求所有禁止事项标注强制等级；宪法 13.0.1-13.0.3 要求测试优先。

---

### 12. 宪法条款映射（新增）

**位置**：文档末尾

**变更类型**：新增

**原因**：便于 AI 快速定位宪法条款与领域规则的对应关系。

---

### 13. 架构违规检测（新增）

**位置**：文档末尾

**变更类型**：新增

**原因**：架构文档定义了标准违规检测模式，领域文档应纳入。

---

## 变更统计

| 强制等级 | 原文档数量 | 新文档数量 |
|----------|-----------|-----------|
| 🟥 绝对禁止 | 0 | 14 |
| 🟩 必须遵守 | 0 | 8 |
| 宪法条款引用 | 0 | 6 |
| 架构违规检测 | 0 | 2 |
