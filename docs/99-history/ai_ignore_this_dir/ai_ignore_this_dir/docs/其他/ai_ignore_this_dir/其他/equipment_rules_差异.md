# equipment_rules.md → equipment_rules_v1.md 更新差异

Version: 1.0 → 1.1

---

## 变更总览

| 变更类型 | 数量 |
|----------|------|
| 新增 | 8 |
| 修改 | 12 |
| 删除 | 0 |

---

## 逐项变更

### 1. 核心原则（修改）

**位置**：核心原则

**变更类型**：修改

**原内容**：
```
- Definition / Instance 分离
- 装备 = Modifier + Trait
- Rule / Content 分离
- 标签三层架构
```

**新内容**：
```
- 🟥 Definition / Instance 分离（宪法 1.1.2）
- 🟩 装备 = Modifier + Trait
- 🟥 Rule / Content 分离（宪法 1.1.3）
- 🟩 标签三层架构
- 🟥 修饰符必须通过 Modifier 管线（宪法 2.2.1）
```

**原因**：宪法要求所有规则标注强制等级；补充宪法条款引用；新增修饰符管线原则。

---

### 2. 术语定义新增 ModifierSource（新增）

**位置**：术语定义

**变更类型**：新增

**新内容**：
```
## ModifierSource

修饰符来源标识。装备领域使用 equipment_source(slot) 变体。

不是修饰符本身。ModifierSource 是来源标识，Modifier 是属性修饰。

关键属性：
- equipment_source(slot)：装备来源，记录具体槽位
```

**原因**：ModifierSource 是装备修饰符追踪的核心概念，原文档在规则中提及但未在术语中定义。

---

### 3. 跨领域通信方式修改（修改）

**位置**：领域边界 → 跨领域通信方式

**变更类型**：修改

**原内容**：
```
| 属性修饰符变化 | 直接修改 Attributes | stat_system |
```

**新内容**：
```
| 属性修饰符变化 | 通过 ModifierSource 添加到 Attributes | stat_system |
```

**原因**：宪法 2.2.1 禁止直接修改属性值，修饰符必须通过 ModifierSource 添加。

---

### 4. 不变量1-4 添加强制等级（修改）

**位置**：不变量1-4

**变更类型**：修改

**原内容**：无强制等级标记

**新内容**：
```
不变量1：穿脱后 Trait 必须重建 🟥
不变量2：修饰符来源精确追踪 🟥
不变量3：标签分层不混淆 🟥
不变量4：槽位唯一性 🟥
```

**原因**：宪法要求所有规则标注强制等级。所有不变量均为🟥绝对禁止级别。

---

### 5. 不变量1 新增架构违规检测（修改）

**位置**：不变量1

**变更类型**：修改

**新增内容**：
```
架构违规检测：
发现穿脱后 TraitCollection 与装备状态不一致时，必须停止。必须输出：
ARCHITECTURE VIOLATION: 穿脱后 TraitCollection 未重建，装备 Trait 与实际状态不一致。
```

**原因**：Trait 重建是核心架构约束，应有明确违规检测。

---

### 6. 不变量2 添加宪法引用（修改）

**位置**：不变量2

**变更类型**：修改

**新增内容**：
```
宪法依据：2.2.1（禁止直接修改最终属性值）
```

**原因**：补充宪法条款引用。

---

### 7. 不变量5：修饰符必须通过 Modifier 管线（新增）

**位置**：不变量

**变更类型**：新增

**新内容**：
```
## 不变量5：修饰符必须通过 Modifier 管线 🟥

宪法依据：2.2.1（禁止直接修改最终属性值）

穿脱操作完成后：
装备的属性修饰必须通过 ModifierSource::equipment_source 添加到 Attributes，禁止直接修改属性值。

违反表现：
装备直接修改 HP、ATK 等最终属性值。
```

**原因**：宪法 2.2.1 明确禁止直接修改最终属性值，这是装备领域的核心不变量。

---

### 8. 规则1-4 添加强制等级和宪法引用（修改）

**位置**：业务规则1-4

**变更类型**：修改

**原内容**：无强制等级标记，无宪法引用

**新内容**：
```
规则1：穿戴 🟥
规则2：脱卸 🟥
规则3：需求检查 🟩
规则4：装备效果应用 🟥
宪法依据：2.2.1（禁止直接修改最终属性值）
```

**原因**：宪法要求所有规则标注强制等级和宪法条款引用。

---

### 9. 规则1-4 禁止事项添加强制等级（修改）

**位置**：规则1-4

**变更类型**：修改

**原内容**：无强制等级标记

**新内容**：
```
规则1禁止：
- 🟥 跳过需求检查
- 🟥 跳过 Trait 重建
- 🟥 不处理旧装备替换

规则2禁止：
- 🟥 跳过修饰符清理
- 🟥 跳过 Trait 重建
- 🟥 不放回背包

规则4禁止：
- 🟥 直接修改角色基础属性
- 🟥 绕过 Modifier 管线
```

**原因**：宪法要求所有禁止事项标注强制等级。

---

### 10. 流程管线步骤添加强制等级（修改）

**位置**：流程管线 → 穿戴管线 Step1-5 / 脱卸管线 Step1-3

**变更类型**：修改

**原内容**：无强制等级标记

**新内容**：
```
穿戴管线：
Step2: 🟥 禁止：检查失败时继续穿戴
Step3: 🟥 禁止：跳过旧装备处理
Step4: 🟥 禁止：绕过 Modifier 管线（宪法 2.2.1）
Step5: 🟥 禁止：跳过重建

脱卸管线：
Step1: 🟥 禁止：遗漏任何修饰符
Step2: 🟥 禁止：丢弃装备
Step3: 🟥 禁止：跳过重建
```

**原因**：宪法要求所有禁止事项标注强制等级。

---

### 11. 数据结构添加强制等级（修改）

**位置**：数据结构 → EquipmentDef / EquipmentInstance / EquipmentSlots

**变更类型**：修改

**新增内容**：
```
EquipmentDef: 🟥 不可变，加载后不修改（宪法 1.1.2）、🟥 RON 配置路径（宪法 1.1.5）
EquipmentInstance: 🟥 创建时 durability = max_durability
EquipmentSlots: 🟥 每个槽位最多一个装备
```

**原因**：宪法要求所有规则标注强制等级和宪法条款引用。

---

### 12. 禁止事项添加强制等级和新增1条（修改+新增）

**位置**：禁止事项

**变更类型**：修改 + 新增

**修改内容**：原有5条禁止事项全部添加🟥标记和宪法引用

**新增内容**：
```
🟥 禁止：运行时修改 EquipmentDef
原因：宪法 1.1.2 Definition 不可变
架构违规检测：ARCHITECTURE VIOLATION: 运行时修改 EquipmentDef...
```

**原因**：宪法 1.1.2 明确要求 Definition 不可变。

---

### 13. AI 修改规则添加强制等级和测试要求（修改）

**位置**：AI 修改规则

**变更类型**：修改

**新增内容**：
```
禁止：
- 🟥 修改穿戴/脱卸流程
- 🟥 修改需求检查逻辑
- 🟥 修改 Trait 重建逻辑
- 🟥 修改现有需求的检查逻辑
- 🟥 改变需求检查顺序
- 🟥 绕过 Modifier 管线
- 🟥 跳过 Trait 重建

测试要求（宪法 13.0.1-13.0.3）：
- 🟩 单元测试：验证穿脱流程各步骤
- 🟩 集成测试：验证完整装备生命周期
- 🟩 Bug 修复必须先编写重现测试（宪法 13.0.2）
```

**原因**：宪法要求所有禁止事项标注强制等级；宪法 13.0.1-13.0.3 要求测试优先。

---

### 14. 宪法条款映射（新增）

**位置**：文档末尾

**变更类型**：新增

**原因**：便于 AI 快速定位宪法条款与领域规则的对应关系。

---

### 15. 架构违规检测（新增）

**位置**：文档末尾

**变更类型**：新增

**原因**：架构文档定义了标准违规检测模式，领域文档应纳入。

---

## 变更统计

| 强制等级 | 原文档数量 | 新文档数量 |
|----------|-----------|-----------|
| 🟥 绝对禁止 | 0 | 16 |
| 🟩 必须遵守 | 0 | 6 |
| 宪法条款引用 | 0 | 6 |
| 架构违规检测 | 0 | 3 |
