# modifier_rules.md → modifier_rules_v1.md 更新差异

Version: 1.0 → 1.1

---

## 变更总览

| 变更类型 | 数量 |
|----------|------|
| 新增 | 5 |
| 修改 | 9 |
| 删除 | 0 |

---

## 逐项变更

### 1. 核心原则（修改）

**位置**：核心原则

**变更类型**：修改

**原内容**：
```
- Rule / Content 分离
- Trait 替代 match
- 标签驱动匹配
- 修饰链可追踪
```

**新内容**：
```
- 🟥 Rule / Content 分离（宪法 1.1.3）
- 🟥 Trait 替代 match（宪法 6.0.2）
- 🟩 标签驱动匹配
- 🟩 修饰链可追踪
```

**原因**：宪法要求所有规则标注强制等级；补充宪法条款引用。

---

### 2. 不变量1-4 添加强制等级（修改）

**位置**：不变量1-4

**变更类型**：修改

**原内容**：无强制等级标记

**新内容**：
```
不变量1：双标签同时匹配 🟥
不变量2：链式修饰顺序固定 🟥
不变量3：伤害下限为 1 🟥
不变量4：治疗下限为 0 🟥
```

**原因**：宪法要求所有规则标注强制等级。所有不变量均为🟥绝对禁止级别。

---

### 3. 不变量5：Calculator 通过 type_name 分发（新增）

**位置**：不变量

**变更类型**：新增

**新内容**：
```
## 不变量5：Calculator 通过 type_name 分发 🟥

宪法依据：6.0.2（Trait 用于扩展点，禁止 match 分发）

效果类型分发时：
必须通过 ModifierCalculator.type_name() 查找对应计算器，禁止 match 分发效果类型。

违反表现：
使用 match effect { DamageMultiplier => ..., DamageBonus => ... } 分发。

架构违规检测：
ARCHITECTURE VIOLATION: match 分发效果类型，违反"Calculator 通过 type_name 分发"原则。
```

**原因**：宪法 6.0.2 明确禁止 match 分发，这是 Modifier 领域的核心不变量。原文档规则3已提及但未提升为不变量。

---

### 4. 规则1-4 添加强制等级和宪法引用（修改）

**位置**：业务规则1-4

**变更类型**：修改

**原内容**：无强制等级标记，无宪法引用

**新内容**：
```
规则1：标签匹配 🟥
规则2：链式修饰 🟥
规则3：Calculator 分发 🟥
宪法依据：6.0.2（Trait 用于扩展点）
规则4：修饰记录 🟩
```

**原因**：宪法要求所有规则标注强制等级和宪法条款引用。

---

### 5. 规则1-3 禁止事项添加强制等级（修改）

**位置**：规则1-3

**变更类型**：修改

**原内容**：无强制等级标记

**新内容**：
```
规则1禁止：
- 🟥 单标签匹配就触发规则
- 🟥 跳过标签检查

规则2禁止：
- 🟥 只应用第一条匹配规则
- 🟥 修改规则应用顺序

规则3禁止：
- 🟥 match 分发效果类型
```

**原因**：宪法要求所有禁止事项标注强制等级。

---

### 6. 流程管线步骤添加强制等级（修改）

**位置**：流程管线 → Step1-4

**变更类型**：修改

**原内容**：无强制等级标记

**新内容**：
```
Step1: 🟥 禁止：跳过任何规则
Step2: 🟥 禁止：单标签匹配
Step3: 🟩 禁止：找不到计算器时 panic
Step4: 🟩 禁止：跳过记录
```

**原因**：宪法要求所有禁止事项标注强制等级。

---

### 7. 数据结构添加强制等级（修改）

**位置**：数据结构 → ModifierRule / ModifierEffect / ModifierCalculator / ModifierEntry

**变更类型**：修改

**新增内容**：
```
ModifierRule: 🟥 RON 配置路径（宪法 1.1.5）、🟥 双标签同时匹配
ModifierCalculator: 🟥 新增效果类型只需实现并注册（宪法 6.0.2）
```

**原因**：宪法要求所有规则标注强制等级和宪法条款引用。

---

### 8. 禁止事项添加强制等级和新增1条（修改+新增）

**位置**：禁止事项

**变更类型**：修改 + 新增

**修改内容**：原有4条禁止事项全部添加🟥标记

**新增内容**：
```
🟥 禁止：硬编码规则逻辑
原因：宪法 1.1.3 Rule/Content 分离
违反后果：新增规则需修改代码
```

**原因**：宪法 1.1.3 明确要求 Rule/Content 分离。

---

### 9. 禁止事项新增架构违规检测（修改）

**位置**：禁止事项 → "match 分发效果类型"

**变更类型**：修改

**新增内容**：
```
架构违规检测：
ARCHITECTURE VIOLATION: match 分发效果类型，违反"Calculator 通过 type_name 分发"原则。
```

**原因**：match 分发是核心架构违规。

---

### 10. AI 修改规则添加强制等级和测试要求（修改）

**位置**：AI 修改规则

**变更类型**：修改

**新增内容**：
```
禁止：
- 🟥 修改修饰管线流程
- 🟥 修改标签匹配逻辑
- 🟥 硬编码规则逻辑

测试要求（宪法 13.0.1-13.0.3）：
- 🟩 单元测试：验证每种 Calculator 计算正确性
- 🟩 集成测试：验证完整修饰管线
- 🟩 Bug 修复必须先编写重现测试（宪法 13.0.2）
```

**原因**：宪法要求所有禁止事项标注强制等级；宪法 13.0.1-13.0.3 要求测试优先。

---

### 11. 宪法条款映射（新增）

**位置**：文档末尾

**变更类型**：新增

**原因**：便于 AI 快速定位宪法条款与领域规则的对应关系。

---

### 12. 架构违规检测（新增）

**位置**：文档末尾

**变更类型**：新增

**原因**：架构文档定义了标准违规检测模式，领域文档应纳入。

---

## 变更统计

| 强制等级 | 原文档数量 | 新文档数量 |
|----------|-----------|-----------|
| 🟥 绝对禁止 | 0 | 11 |
| 🟩 必须遵守 | 0 | 6 |
| 宪法条款引用 | 0 | 4 |
| 架构违规检测 | 0 | 2 |
