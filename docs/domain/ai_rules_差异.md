# ai_rules.md → ai_rules_v1.md 更新差异

Version: 1.0 → 1.1

---

## 变更总览

| 变更类型 | 数量 |
|----------|------|
| 新增 | 12 |
| 修改 | 15 |
| 删除 | 0 |

---

## 逐项变更

### 1. 核心原则（修改）

**位置**：核心原则

**变更类型**：修改

**原内容**：
```
- Trait 描述规则，不描述内容
- 数据驱动
- 注册表分发
- Rule / Content 分离
```

**新内容**：
```
- 🟩 Trait 描述规则，不描述内容（宪法 6.0.1）
- 🟩 数据驱动（宪法 1.1.5）
- 🟩 注册表分发
- 🟥 Rule / Content 分离（宪法 1.1.3）
- 🟩 组合优于继承（宪法 1.1.6）
```

**原因**：宪法要求所有规则标注强制等级；补充宪法条款引用；Rule/Content 分离为🟥绝对禁止级别；新增"组合优于继承"原则（宪法 1.1.6）。

---

### 2. 术语定义新增 CombatIntent（新增）

**位置**：术语定义

**变更类型**：新增

**新内容**：
```
## CombatIntent

攻击意图组件，AI 和玩家共用，由 Effect Pipeline 统一处理。

不是 AI 专属组件。CombatIntent 是唯一攻击意图通道（宪法 7.0.5，架构 AI 模块边界）。
```

**原因**：架构 AI 模块边界明确规定"CombatIntent 是唯一攻击意图通道"，术语表中应体现。

---

### 3. 领域边界新增"伤害计算"（修改）

**位置**：领域边界 → 本领域不负责

**变更类型**：修改

**原内容**：
```
- 效果管线执行（由 effect_pipeline 领域负责）
- 寻路计算（由 map_rules 领域负责）
- 回合推进（由 turn_rules 领域负责）
- UI 展示（由 ui_rules 领域负责）
```

**新内容**：
```
- 效果管线执行（由 effect_pipeline 领域负责）
- 寻路计算（由 map_rules 领域负责）
- 回合推进（由 turn_rules 领域负责）
- UI 展示（由 ui_rules 领域负责）
- 伤害计算（由 effect_pipeline 领域负责）
```

**原因**：架构 AI 模块边界明确规定 AI 不可独立计算伤害，领域边界应明确排除。

---

### 4. 跨领域通信方式修改（修改）

**位置**：领域边界 → 跨领域通信方式

**变更类型**：修改

**原内容**：
```
| 攻击意图 | CombatIntent | battle |
| 移动意图 | MovingUnit | battle |
```

**新内容**：
```
| 攻击意图 | CombatIntent 组件 | battle |
| 移动意图 | MovingUnit 组件 | battle |
```

**原因**：明确通信方式为组件而非 Message，与架构一致。

---

### 5. 不变量1 新增强制等级和架构违规检测（修改）

**位置**：不变量1

**变更类型**：修改

**原内容**：
```
## 不变量1：AI 和玩家共用 Effect Pipeline

任意时刻：
AI 设置的 CombatIntent 与玩家设置的走同一条效果管线。

违反表现：
AI 攻击绕过效果管线，直接扣血。
```

**新内容**：
```
## 不变量1：AI 和玩家共用 Effect Pipeline 🟥

任意时刻：
AI 设置的 CombatIntent 与玩家设置的走同一条效果管线。

违反表现：
AI 攻击绕过效果管线，直接扣血。

架构违规检测：
发现 AI 模块包含独立伤害计算时，必须停止。必须输出：
ARCHITECTURE VIOLATION: AI 模块包含独立伤害计算，违反"AI 与玩家共享 Effect Pipeline"原则。
```

**原因**：架构 AI 模块边界定义了明确的违规检测模式，必须纳入；此为🟥绝对禁止级别。

---

### 6. 不变量2 新增 CombatIntent 唯一通道（新增）

**位置**：不变量

**变更类型**：新增

**新内容**：
```
## 不变量2：CombatIntent 是唯一攻击意图通道 🟥

任意时刻：
AI 的攻击意图只能通过 CombatIntent 组件表达，禁止使用其他方式触发攻击。

违反表现：
AI 直接调用伤害函数或创建独立攻击逻辑。
```

**原因**：架构 AI 模块边界明确规定"CombatIntent 是唯一攻击意图通道"，原不变量2（UnitSnapshot）降为不变量3。

---

### 7. 原不变量2-4 添加强制等级（修改）

**位置**：不变量2-5

**变更类型**：修改

**原内容**：无强制等级标记

**新内容**：
```
不变量3：UnitSnapshot 避免借用冲突 🟩
不变量4：策略名称与 RON 对应 🟩
不变量5：技能冷却检查 🟩
```

**原因**：宪法要求所有规则标注强制等级。

---

### 8. 规则1-4 添加强制等级和宪法引用（修改）

**位置**：业务规则1-4

**变更类型**：修改

**原内容**：无强制等级标记，无宪法引用

**新内容**：
```
规则1：策略 trait 替代 enum+match 🟩
宪法依据：6.0.2（Trait 用于扩展点）

规则2：AI 决策流程 🟩
规则3：行动结果 🟩
规则4：CautiousMove 保持距离 🟩
```

**原因**：宪法要求所有规则标注强制等级和宪法条款引用。

---

### 9. 规则5：Rule / Content 分离（新增）

**位置**：业务规则

**变更类型**：新增

**新内容**：
```
## 规则5：Rule / Content 分离 🟥

宪法依据：1.1.3（规则与内容强制分离）

禁止：
- 🟥 新增 AI 行为时修改核心规则代码
- 🟥 硬编码行为逻辑

必须：
- 新增 AI 行为只修改 RON 配置
- 新增 AI 策略只需实现 trait 并注册

架构违规检测：
ARCHITECTURE VIOLATION: 新增内容修改了规则代码 [文件名]，违反 Rule/Content 分离原则。
```

**原因**：宪法 1.1.3 为🟥绝对禁止级别，原文档虽提及"Rule / Content 分离"但未作为独立业务规则，且缺少强制等级和违规检测。

---

### 10. 流程管线步骤添加强制等级（修改）

**位置**：流程管线 → Step1-4

**变更类型**：修改

**原内容**：
```
禁止：修改 HP、发送 Message、跳过前置检查
禁止：持有 ECS 可变引用
禁止：跳过任何策略步骤
禁止：直接执行效果
```

**新内容**：
```
🟥 禁止：跳过计时器
🟥 禁止：持有 ECS 可变引用（宪法 2.1.2）
🟩 禁止：跳过任何策略步骤
🟥 禁止：直接执行效果（必须走 Effect Pipeline）
```

**原因**：宪法要求所有禁止事项标注强制等级；补充宪法条款引用；区分🟥绝对禁止和🟩必须遵守。

---

### 11. 数据结构添加强制等级（修改）

**位置**：数据结构 → AiBehavior / TargetSelector / MoveSelector / SkillSelector / UnitSnapshot

**变更类型**：修改

**原内容**：无强制等级标记

**新内容**：
```
AiBehavior: 🟥 不可变，加载后不修改（宪法 1.1.2）
TargetSelector: 🟩 用于定义需要扩展的接口（宪法 6.0.2）
MoveSelector: 🟩 用于定义需要扩展的接口（宪法 6.0.2）
SkillSelector: 🟩 用于定义需要扩展的接口（宪法 6.0.2）
UnitSnapshot: 🟩 纯数据，不持有 ECS 引用（宪法 2.1.2）
UnitSnapshot: 🟩 Entity 仅作为 ID 传递（宪法 2.1.1）
```

**原因**：宪法要求所有规则标注强制等级和宪法条款引用；Definition 不可变为🟥级别。

---

### 12. 禁止事项新增两条🟥级别（新增+修改）

**位置**：禁止事项

**变更类型**：新增 + 修改

**新增内容**：
```
🟥 禁止：AI 独立执行攻击逻辑
架构违规检测：ARCHITECTURE VIOLATION: AI 模块包含独立伤害计算，违反"AI 与玩家共享 Effect Pipeline"原则。

🟥 禁止：AI 独立计算伤害
架构违规检测：ARCHITECTURE VIOLATION: AI 模块包含独立伤害计算，违反"AI 与玩家共享 Effect Pipeline"原则。

🟥 禁止：硬编码 AI 行为逻辑
原因：Rule / Content 分离（宪法 1.1.3）
```

**修改内容**：原有禁止事项全部添加强制等级标记：
```
🟥 禁止：match 分发策略类型（宪法 6.0.2）
🟥 禁止：跳过技能冷却检查
🟥 禁止：AI 直接执行效果（宪法 1.1.4）
🟥 禁止：AI 系统持有 ECS 可变引用（宪法 2.1.2）
🟩 禁止：跳过 AI 计时器
```

**原因**：架构 AI 模块边界明确列出"独立执行攻击逻辑"和"独立计算伤害"为🟥绝对禁止；宪法要求所有禁止事项标注强制等级。

---

### 13. AI 修改规则添加强制等级和测试要求（修改）

**位置**：AI 修改规则

**变更类型**：修改

**新增内容**：
```
- 🟥 修改 enemy_ai_system 流程
- 🟥 修改 UnitSnapshot 结构（除非必要）
- 🟥 硬编码行为逻辑
- 🟥 修改核心规则代码
- 🟥 跳过策略查找直接硬编码
- 🟥 直接执行效果
- 🟥 独立计算伤害
- 🟩 是否重复出现三次以上才抽象为 Trait（宪法 6.0.3）
```

**新增测试要求**：
```
测试要求（宪法 13.0.1-13.0.3）：
- 🟩 单元测试：验证策略选择逻辑
- 🟩 集成测试：验证完整 AI 决策流程
- 🟩 Bug 修复必须先编写重现测试（宪法 13.0.2）
```

**原因**：宪法要求所有禁止事项标注强制等级；宪法 13.0.1-13.0.3 要求测试优先；宪法 6.0.3 要求 Trait 抽象原则。

---

### 14. 宪法条款映射（新增）

**位置**：文档末尾

**变更类型**：新增

**新内容**：
```
| 宪法条款 | 本领域对应 |
|----------|-----------|
| 1.1.2 Definition/Instance 分离 | AiBehavior(Definition) vs AiStrategyRegistry(Instance) |
| 1.1.3 Rule/Content 分离 | 策略 trait 是规则，RON 配置是内容 |
| 1.1.4 Logic/Presentation 分离 | AI 决策不包含 UI/动画逻辑 |
| 1.1.5 数据驱动 | AiBehavior 从 RON 加载 |
| 1.1.6 组合优于继承 | 策略组合而非继承树 |
| 2.1.1 Entity 只是 ID | UnitSnapshot 中 Entity 仅作 ID |
| 2.1.2 数据与行为分离 | UnitSnapshot 纯数据 |
| 6.0.2 Trait 用于扩展点 | TargetSelector/MoveSelector/SkillSelector |
| 6.0.3 Trait 抽象原则 | 策略出现三次以上才抽象 |
| 7.0.5 战斗事件链 | CombatIntent 是唯一攻击意图通道 |
```

**原因**：便于 AI 快速定位宪法条款与领域规则的对应关系，确保修改时能追溯宪法依据。

---

### 15. 架构违规检测（新增）

**位置**：文档末尾

**变更类型**：新增

**新内容**：
```
| 违规行为 | 检测方式 | 输出 |
|----------|----------|------|
| AI 模块包含独立伤害计算 | 代码审查 | ARCHITECTURE VIOLATION: AI 模块包含独立伤害计算... |
| AI 直接修改 ECS 业务状态 | 代码审查 | ARCHITECTURE VIOLATION: AI 直接修改业务状态... |
| 新增 AI 行为修改规则代码 | 代码审查 | ARCHITECTURE VIOLATION: 新增内容修改了规则代码... |
```

**原因**：架构文档定义了标准违规检测模式，领域文档应纳入以确保一致性。

---

## 变更统计

| 强制等级 | 原文档数量 | 新文档数量 |
|----------|-----------|-----------|
| 🟥 绝对禁止 | 0 | 10 |
| 🟩 必须遵守 | 0 | 8 |
| 宪法条款引用 | 0 | 10 |
| 架构违规检测 | 0 | 3 |
