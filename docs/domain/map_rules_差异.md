# map_rules.md → map_rules_v1.md 更新差异

Version: 1.0 → 1.1

---

## 变更总览

| 变更类型 | 数量 |
|----------|------|
| 新增 | 5 |
| 修改 | 10 |
| 删除 | 0 |

---

## 逐项变更

### 1. 核心原则（修改）

**位置**：核心原则

**变更类型**：修改

**原内容**：
```
- 地图优先看成 Grid 数据
- 地图数据与渲染分离
- OccupancyGrid 独立存在
- 寻路数据运行时生成
- 数据驱动
```

**新内容**：
```
- 🟩 地图优先看成 Grid 数据
- 🟥 地图数据与渲染分离（宪法 1.1.4）
- 🟥 OccupancyGrid 独立存在
- 🟩 寻路数据运行时生成
- 🟥 数据驱动（宪法 1.1.5）
```

**原因**：宪法要求所有规则标注强制等级；补充宪法条款引用。

---

### 2. 不变量1-5 添加强制等级（修改）

**位置**：不变量1-5

**变更类型**：修改

**原内容**：无强制等级标记

**新内容**：
```
不变量1：TerrainGrid 是地形唯一真相源 🟥
不变量2：OccupancyGrid 是占位唯一真相源 🟥
不变量3：未配置格子默认 plain 🟩
不变量4：占用排除自身 🟥
不变量5：成本计算器标签优先级 🟩
```

**原因**：宪法要求所有规则标注强制等级。

---

### 3. 不变量1 新增宪法引用和架构违规检测（修改）

**位置**：不变量1

**变更类型**：修改

**新增内容**：
```
宪法依据：2.1.5（禁止把 Resource 当全局变量仓库——但 TerrainGrid 是唯一真相源，不是仓库）

架构违规检测：
发现 Tile Entity 存储地形数据时，必须停止。必须输出：
ARCHITECTURE VIOLATION: Tile Entity 存储地形数据，违反 TerrainGrid 唯一真相源原则。
```

**原因**：补充宪法条款引用；唯一真相源是核心架构约束，应有明确违规检测。

---

### 4. 规则1-4 添加强制等级和宪法引用（修改）

**位置**：业务规则1-4

**变更类型**：修改

**原内容**：无强制等级标记，无宪法引用

**新内容**：
```
规则1：地形数据与渲染分离 🟥
宪法依据：1.1.4（逻辑与表现强制分离）

规则2：寻路 🟥

规则3：路径回溯 🟩

规则4：成本计算器扩展 🟩
宪法依据：6.0.2（Trait 用于扩展点）
```

**原因**：宪法要求所有规则标注强制等级和宪法条款引用。

---

### 5. 规则1-4 禁止事项添加强制等级（修改）

**位置**：规则1-4

**变更类型**：修改

**原内容**：无强制等级标记

**新内容**：
```
规则1禁止：
- 🟥 Tile Entity 存储地形数据
- 🟥 渲染系统修改 TerrainGrid

规则2禁止：
- 🟥 预存路径
- 🟥 跳过占用检查
- 🟥 跳过地形成本计算

规则4禁止：
- 🟥 match 分发单位类型
```

**原因**：宪法要求所有禁止事项标注强制等级。

---

### 6. 流程管线步骤添加强制等级（修改）

**位置**：流程管线 → 地图生成管线 Step1-2 / 寻路管线 Step1-2

**变更类型**：修改

**原内容**：无强制等级标记

**新内容**：
```
地图生成管线：
Step1: 🟥 禁止：生成 Tile Entity
Step2: 🟥 禁止：修改 TerrainGrid

寻路管线：
Step1: 🟥 禁止：跳过占用检查、跳过成本计算
Step2: 🟩 禁止：target 不在 reachable 时返回空
```

**原因**：宪法要求所有禁止事项标注强制等级。

---

### 7. 数据结构添加强制等级（修改）

**位置**：数据结构 → TerrainGrid / OccupancyGrid / GameMap / TerrainDef / TerrainCostCalculator

**变更类型**：修改

**新增内容**：
```
TerrainGrid: 🟥 不生成 Tile Entity
OccupancyGrid: 🟥 寻路时排除自身
TerrainDef: 🟥 RON 配置路径（宪法 1.1.5）
```

**原因**：宪法要求所有规则标注强制等级和宪法条款引用。

---

### 8. 禁止事项添加强制等级和新增1条（修改+新增）

**位置**：禁止事项

**变更类型**：修改 + 新增

**修改内容**：原有5条禁止事项全部添加🟥标记和宪法引用

**新增内容**：
```
🟥 禁止：match 分发单位类型计算成本
原因：宪法 6.0.2 要求通过 Trait 扩展，禁止 match 分发
违反后果：新增单位类型需修改寻路代码
```

**原因**：规则4已禁止 match 分发，禁止事项应同步。

---

### 9. 禁止事项新增架构违规检测（修改）

**位置**：禁止事项 → "Tile Entity 存储地形数据"

**变更类型**：修改

**新增内容**：
```
架构违规检测：
ARCHITECTURE VIOLATION: Tile Entity 存储地形数据，违反 TerrainGrid 唯一真相源原则。
```

**原因**：唯一真相源是核心架构约束。

---

### 10. AI 修改规则添加强制等级和测试要求（修改）

**位置**：AI 修改规则

**变更类型**：修改

**新增内容**：
```
禁止：
- 🟥 修改 TerrainGrid 结构
- 🟥 修改 OccupancyGrid 结构
- 🟥 修改 BFS 算法
- 🟥 硬编码关卡数据

测试要求（宪法 13.0.1-13.0.3）：
- 🟩 单元测试：验证寻路算法正确性
- 🟩 集成测试：验证地图生成和渲染
- 🟩 Bug 修复必须先编写重现测试（宪法 13.0.2）
```

**原因**：宪法要求所有禁止事项标注强制等级；宪法 13.0.1-13.0.3 要求测试优先。

---

### 11. 宪法条款映射（新增）

**位置**：文档末尾

**变更类型**：新增

**原因**：便于 AI 快速定位宪法条款与领域规则的对应关系。

---

### 12. 架构违规检测（新增）

**位置**：文档末尾

**变更类型**：新增

**原因**：架构文档定义了标准违规检测模式，领域文档应纳入。

---

## 变更统计

| 强制等级 | 原文档数量 | 新文档数量 |
|----------|-----------|-----------|
| 🟥 绝对禁止 | 0 | 13 |
| 🟩 必须遵守 | 0 | 7 |
| 宪法条款引用 | 0 | 5 |
| 架构违规检测 | 0 | 3 |
