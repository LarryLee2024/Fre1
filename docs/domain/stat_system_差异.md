# stat_system.md → stat_system_v1.md 更新差异

Version: 1.0 → 1.1

---

## 变更总览

| 变更类型 | 数量 |
|----------|------|
| 新增 | 4 |
| 修改 | 12 |
| 删除 | 0 |

---

## 逐项变更

### 1. 核心原则（修改）

**位置**：核心原则

**变更类型**：修改

**原内容**：
```
- Primary Stat 与 Derived Stat 分离
- Derived Stat 优先实时计算
- 统一 Modifier 管线
- 属性公式集中管理
```

**新内容**：
```
- 🟥 Primary Stat 与 Derived Stat 分离
- 🟩 Derived Stat 优先实时计算
- 🟥 统一 Modifier 管线（宪法 2.2.1）
- 🟩 属性公式集中管理
```

**原因**：宪法要求所有规则标注强制等级；补充宪法条款引用。

---

### 2. 跨领域通信方式修改（修改）

**位置**：领域边界 → 跨领域通信方式

**变更类型**：修改

**原内容**：
```
| 属性修饰符变化 | 直接函数调用 | character |
| HP 变化 | 直接修改 | battle |
```

**新内容**：
```
| 属性修饰符变化 | 通过 ModifierSource 添加/移除 | character |
| HP 变化 | 通过 set_vital() 修改 | battle |
```

**原因**：宪法 2.2.1 禁止直接修改属性值，必须通过 Modifier 管线或 set_vital()。

---

### 3. 不变量1-6 添加强制等级（修改）

**位置**：不变量1-6

**变更类型**：修改

**原内容**：无强制等级标记

**新内容**：
```
不变量1：衍生属性无基础值 🟥
不变量2：先加后乘 🟥
不变量3：乘法零值保护 🟥
不变量4：伤害/治疗下限 🟥
不变量5：Source 区间隔离 🟥
不变量6：三类属性互斥 🟥
```

**原因**：宪法要求所有规则标注强制等级。所有不变量均为🟥绝对禁止级别。

---

### 4. 不变量7：禁止直接修改最终属性值（新增）

**位置**：不变量

**变更类型**：新增

**新内容**：
```
## 不变量7：禁止直接修改最终属性值 🟥

宪法依据：2.2.1（禁止直接修改最终属性值）

任意时刻：
所有属性修改必须通过 Modifier 管线，禁止直接修改 get() 返回的最终值。

违反表现：
代码中存在 `attributes.attack = 50` 或 `hp -= damage` 等直接赋值。

架构违规检测：
ARCHITECTURE VIOLATION: 直接修改最终属性值，违反"属性修改必须通过 Modifier 管线"原则。
```

**原因**：宪法 2.2.1 是 Stat System 的核心约束，必须作为不变量明确声明。

---

### 5. 规则1-4 添加强制等级和宪法引用（修改）

**位置**：业务规则1-4

**变更类型**：修改

**原内容**：无强制等级标记，无宪法引用

**新内容**：
```
规则1：属性访问 🟥
宪法依据：2.2.1（禁止直接修改最终属性值）

规则2：修饰符管理 🟥

规则3：ModifierRule 标签匹配 🟥
宪法依据：1.1.3（Rule/Content 分离）、6.0.2（Trait 替代 match）

规则4：fill_vital_resources 🟩
```

**原因**：宪法要求所有规则标注强制等级和宪法条款引用。

---

### 6. 规则1-3 禁止事项添加强制等级（修改）

**位置**：规则1-3

**变更类型**：修改

**原内容**：无强制等级标记

**新内容**：
```
规则1禁止：
- 🟥 直接修改最终属性值
- 🟥 对衍生属性调用 set_base()
- 🟥 对非 Vital 调用 set_vital()

规则2禁止：
- 🟥 绕过 Modifier 管线直接改属性
- 🟥 修饰符无 Source 标识
- 🟥 修饰符无过期条件

规则3禁止：
- 🟥 硬编码 if-else 替代 ModifierRule
- 🟥 match 分发效果类型
```

**原因**：宪法要求所有禁止事项标注强制等级。

---

### 7. 流程管线步骤添加强制等级（修改）

**位置**：流程管线 → 修饰符计算管线 Step1-4 / ModifierRule 应用管线 Step1-4

**变更类型**：修改

**原内容**：无强制等级标记

**新内容**：
```
修饰符计算管线：
Step1: 🟥 禁止：对 Derived Stat 返回 base 值
Step2: 🟥 禁止：跳过任何 Add 修饰符
Step3: 🟥 禁止：乘积为 0 时返回 0
Step4: 🟥 禁止：跳过下限保护

ModifierRule 应用管线：
Step1: 🟥 禁止：跳过任何规则
Step2: 🟥 禁止：绕过 Calculator 直接计算
Step3: 🟩 禁止：在非 with_breakdown 模式下记录
Step4: 🟥 禁止：跳过下限保护
```

**原因**：宪法要求所有禁止事项标注强制等级。

---

### 8. 数据结构添加强制等级（修改）

**位置**：数据结构 → Attributes / AttributeModifierDef / AttributeModifierInstance / ModifierRule / ModifierEntry

**变更类型**：修改

**新增内容**：
```
Attributes: 🟥 get(kind) 统一访问接口、🟥 衍生属性通过公式实时计算、🟥 修饰符按先加后乘顺序计算
AttributeModifierInstance: 🟥 Source 必须在对应区间内、🟥 按来源精确移除
ModifierRule: 🟥 标签匹配使用 AND 逻辑、🟥 RON 配置路径（宪法 1.1.5）
```

**原因**：宪法要求所有规则标注强制等级和宪法条款引用。

---

### 9. 禁止事项添加强制等级和宪法引用（修改）

**位置**：禁止事项

**变更类型**：修改

**原内容**：无强制等级标记，无宪法引用

**新内容**：7条禁止事项全部添加🟥标记和宪法引用

```
🟥 禁止：直接修改最终属性值（宪法 2.2.1）
🟥 禁止：对衍生属性调用 set_base()
🟥 禁止：绕过 Modifier 管线直接改属性（宪法 2.2.1）
🟥 禁止：修饰符无 Source 标识
🟥 禁止：乘法修饰符乘积归零
🟥 禁止：硬编码 if-else 替代 ModifierRule（宪法 1.1.3）
🟥 禁止：match 分发效果类型（宪法 6.0.2）
```

**原因**：宪法要求所有禁止事项标注强制等级和宪法条款引用。

---

### 10. 禁止事项新增架构违规检测（修改）

**位置**：禁止事项 → "直接修改最终属性值" 和 "match 分发效果类型"

**变更类型**：修改

**新增内容**：
```
直接修改最终属性值：
ARCHITECTURE VIOLATION: 直接修改最终属性值，违反"属性修改必须通过 Modifier 管线"原则。

match 分发效果类型：
ARCHITECTURE VIOLATION: match 分发效果类型，违反"Calculator 通过 type_name 分发"原则。
```

**原因**：核心架构违规应有明确检测模式。

---

### 11. AI 修改规则添加强制等级和测试要求（修改）

**位置**：AI 修改规则

**变更类型**：修改

**新增内容**：
```
禁止：
- 🟥 修改三类互斥规则
- 🟥 修改先加后乘计算顺序
- 🟥 跳过下限保护
- 🟥 修改现有 Calculator 的计算逻辑
- 🟥 修改 ModifierRule 应用管线流程
- 🟥 修改 ModifierRule 应用管线代码
- 🟥 修改 Calculator 分发逻辑

测试要求（宪法 13.0.1-13.0.3）：
- 🟩 单元测试：验证属性计算公式正确性
- 🟩 集成测试：验证完整修饰符管线
- 🟩 Bug 修复必须先编写重现测试（宪法 13.0.2）
```

**原因**：宪法要求所有禁止事项标注强制等级；宪法 13.0.1-13.0.3 要求测试优先。

---

### 12. 宪法条款映射（新增）

**位置**：文档末尾

**变更类型**：新增

**原因**：便于 AI 快速定位宪法条款与领域规则的对应关系。

---

### 13. 架构违规检测（新增）

**位置**：文档末尾

**变更类型**：新增

**原因**：架构文档定义了标准违规检测模式，领域文档应纳入。

---

## 变更统计

| 强制等级 | 原文档数量 | 新文档数量 |
|----------|-----------|-----------|
| 🟥 绝对禁止 | 0 | 18 |
| 🟩 必须遵守 | 0 | 8 |
| 宪法条款引用 | 0 | 5 |
| 架构违规检测 | 0 | 2 |
