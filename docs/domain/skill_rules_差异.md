# skill_rules.md → skill_rules_v1.md 更新差异

Version: 1.0 → 1.1

---

## 变更总览

| 变更类型 | 数量 |
|----------|------|
| 新增 | 5 |
| 修改 | 10 |
| 删除 | 0 |

---

## 逐项变更

### 1. 核心原则（修改）

**位置**：核心原则

**变更类型**：修改

**原内容**：
```
- Definition / Instance 分离
- Rule / Content 分离
- 数据驱动
- 纯函数校验
```

**新内容**：
```
- 🟥 Definition / Instance 分离（宪法 1.1.2）
- 🟥 Rule / Content 分离（宪法 1.1.3）
- 🟥 数据驱动（宪法 1.1.5）
- 🟥 纯函数校验（宪法 2.1.2 数据与行为分离）
```

**原因**：宪法要求所有规则标注强制等级；补充宪法条款引用。

---

### 2. 不变量1-4 添加强制等级和宪法引用（修改）

**位置**：不变量1-4

**变更类型**：修改

**原内容**：无强制等级标记，无宪法引用

**新内容**：
```
不变量1：can_use 纯函数 🟥
宪法依据：2.1.2（数据与行为分离）

不变量2：冷却归零自动清理 🟥

不变量3：set(0) 不产生记录 🟩

不变量4：预览不修改状态 🟥
宪法依据：1.1.4（逻辑与表现分离）
```

**原因**：宪法要求所有规则标注强制等级和宪法条款引用。

---

### 3. 不变量4 新增架构违规检测（修改）

**位置**：不变量4

**变更类型**：修改

**新增内容**：
```
架构违规检测：
发现预览修改 ECS 状态时，必须停止。必须输出：
ARCHITECTURE VIOLATION: 预览修改 ECS 状态，违反"预览是纯函数"原则。
```

**原因**：预览修改状态是核心架构违规，应有明确检测模式。

---

### 4. 规则1-4 添加强制等级（修改）

**位置**：业务规则1-4

**变更类型**：修改

**原内容**：无强制等级标记

**新内容**：
```
规则1：技能定义 🟥
规则2：条件校验 🟥
规则3：冷却管理 🟥
规则4：有效射程 🟩
```

**原因**：宪法要求所有规则标注强制等级。

---

### 5. 规则1-3 禁止事项添加强制等级（修改）

**位置**：规则1-3

**变更类型**：修改

**原内容**：无强制等级标记

**新内容**：
```
规则1禁止：
- 🟥 硬编码技能效果
- 🟥 运行时修改 SkillData

规则2禁止：
- 🟥 跳过冷却检查
- 🟥 can_use() 中修改状态

规则3禁止：
- 🟥 跳过冷却直接使用技能
- 🟥 冷却不递减
```

**原因**：宪法要求所有禁止事项标注强制等级。

---

### 6. 流程管线步骤添加强制等级（修改）

**位置**：流程管线 → 条件校验管线 Step1-2 / 预览管线 Step1-2

**变更类型**：修改

**原内容**：无强制等级标记

**新内容**：
```
条件校验管线：
Step1: 🟥 禁止：修改冷却值
Step2: 🟥 禁止：修改任何状态

预览管线：
Step1: 🟥 禁止：修改 ECS 状态
Step2: 🟥 禁止：修改 ECS 状态
```

**原因**：宪法要求所有禁止事项标注强制等级。

---

### 7. 数据结构添加强制等级（修改）

**位置**：数据结构 → SkillData / SkillSlots / SkillCooldowns / SkillCondition / SkillPreview

**变更类型**：修改

**新增内容**：
```
SkillData: 🟥 不可变，加载后不修改（宪法 1.1.2）、🟥 RON 配置路径（宪法 1.1.5）
SkillCooldowns: 🟥 tick() 归零后移除
SkillPreview: 🟥 纯函数生成，不修改状态
```

**原因**：宪法要求所有规则标注强制等级和宪法条款引用。

---

### 8. 禁止事项添加强制等级和宪法引用（修改）

**位置**：禁止事项

**变更类型**：修改

**原内容**：无强制等级标记，无宪法引用

**新内容**：5条禁止事项全部添加🟥标记和宪法引用

```
🟥 禁止：硬编码技能效果（宪法 1.1.3）
🟥 禁止：跳过冷却检查
🟥 禁止：can_use() 中修改状态（宪法 2.1.2）
🟥 禁止：预览修改状态（宪法 1.1.4）
🟥 禁止：运行时修改 SkillData（宪法 1.1.2）
```

**原因**：宪法要求所有禁止事项标注强制等级和宪法条款引用。

---

### 9. 禁止事项新增架构违规检测（修改）

**位置**：禁止事项 → "预览修改状态" 和 "运行时修改 SkillData"

**变更类型**：修改

**新增内容**：
```
预览修改状态：
ARCHITECTURE VIOLATION: 预览修改 ECS 状态，违反"预览是纯函数"原则。

运行时修改 SkillData：
ARCHITECTURE VIOLATION: 运行时修改 SkillData，违反 Definition/Instance 分离原则。
```

**原因**：核心架构违规应有明确检测模式。

---

### 10. AI 修改规则添加强制等级和测试要求（修改）

**位置**：AI 修改规则

**变更类型**：修改

**新增内容**：
```
禁止：
- 🟥 修改 SkillData 结构
- 🟥 修改 can_use() 逻辑
- 🟥 修改 SkillRegistry 加载流程
- 🟥 修改现有条件的逻辑
- 🟥 改变条件检查顺序
- 🟥 修改 preview_skill_effects 流程
- 🟥 修改 generate_combat_effects 流程

测试要求（宪法 13.0.1-13.0.3）：
- 🟩 单元测试：验证 can_use() 条件校验
- 🟩 集成测试：验证完整技能使用流程
- 🟩 Bug 修复必须先编写重现测试（宪法 13.0.2）
```

**原因**：宪法要求所有禁止事项标注强制等级；宪法 13.0.1-13.0.3 要求测试优先。

---

### 11. 宪法条款映射（新增）

**位置**：文档末尾

**变更类型**：新增

**原因**：便于 AI 快速定位宪法条款与领域规则的对应关系。

---

### 12. 架构违规检测（新增）

**位置**：文档末尾

**变更类型**：新增

**原因**：架构文档定义了标准违规检测模式，领域文档应纳入。

---

## 变更统计

| 强制等级 | 原文档数量 | 新文档数量 |
|----------|-----------|-----------|
| 🟥 绝对禁止 | 0 | 13 |
| 🟩 必须遵守 | 0 | 7 |
| 宪法条款引用 | 0 | 5 |
| 架构违规检测 | 0 | 2 |
