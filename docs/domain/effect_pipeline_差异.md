# effect_pipeline.md → effect_pipeline_v1.md 更新差异

Version: 1.0 → 1.1

---

## 变更总览

| 变更类型 | 数量 |
|----------|------|
| 新增 | 7 |
| 修改 | 12 |
| 删除 | 0 |

---

## 逐项变更

### 1. 核心原则（修改）

**位置**：核心原则

**变更类型**：修改

**原内容**：
```
- ECS 是数据流，不是调用链
- Handler 分发扩展
- Logic / Presentation 分离
- 数据驱动
```

**新内容**：
```
- 🟥 ECS 是数据流，不是调用链（宪法 2.3.1）
- 🟩 Handler 分发扩展（宪法 6.0.2）
- 🟥 Logic / Presentation 分离（宪法 1.1.4）
- 🟥 数据驱动（宪法 1.1.5）
- 🟥 Rule / Content 分离（宪法 1.1.3）
```

**原因**：宪法要求所有规则标注强制等级；补充宪法条款引用；新增 Rule/Content 分离原则。

---

### 2. 不变量1-5 添加强制等级（修改）

**位置**：不变量1-5

**变更类型**：修改

**原内容**：无强制等级标记

**新内容**：
```
不变量1：管线严格顺序 🟥
不变量2：EffectQueue 执行后清空 🟥
不变量3：伤害下限 🟥
不变量4：base_amount 首次记录 🟩
不变量5：Generate 不修改 ECS 🟥
```

**原因**：宪法要求所有规则标注强制等级。

---

### 3. 不变量1 新增架构违规检测（修改）

**位置**：不变量1

**变更类型**：修改

**新增内容**：
```
架构违规检测：
发现效果处理跳步时，必须停止。必须输出：
ARCHITECTURE VIOLATION: 效果管线跳步 [从 XXX 直接到 XXX]，违反 Generate → Modify → Execute 严格顺序。
```

**原因**：管线跳步是核心架构违规，应有明确检测模式。

---

### 4. 不变量5 添加宪法引用（修改）

**位置**：不变量5

**变更类型**：修改

**新增内容**：
```
宪法依据：2.3.1（ECS 是数据流）、1.1.4（Logic/Presentation 分离）
```

**原因**：补充宪法条款引用。

---

### 5. 不变量6：属性修改必须通过 Modifier（新增）

**位置**：不变量

**变更类型**：新增

**新内容**：
```
## 不变量6：属性修改必须通过 Modifier 🟥

宪法依据：2.2.1（禁止直接修改最终属性值）

Modify 阶段完成后：
伤害/治疗修饰必须通过 ModifierRule 管线，禁止直接修改最终属性值。

违反表现：
在 Generate 或 Modify 阶段直接修改 HP 等最终属性值。
```

**原因**：宪法 2.2.1 明确禁止直接修改最终属性值，这是 Effect Pipeline 的核心不变量。

---

### 6. 规则1-4 添加强制等级和宪法引用（修改）

**位置**：业务规则1-4

**变更类型**：修改

**原内容**：无强制等级标记，无宪法引用

**新内容**：
```
规则1：Generate 🟥
规则2：Modify 🟥
规则3：Execute 🟥
规则4：Handler 分发 🟩
宪法依据：6.0.2（Trait 用于扩展点）
```

**原因**：宪法要求所有规则标注强制等级和宪法条款引用。

---

### 7. 规则1-3 禁止事项添加强制等级（修改）

**位置**：规则1-3

**变更类型**：修改

**原内容**：无强制等级标记

**新内容**：
```
规则1禁止：
- 🟥 修改 ECS 状态（除 EffectQueue）
- 🟥 跳过前置检查（晕眩/冷却）
- 🟥 直接执行效果

规则2禁止：
- 🟥 修改 ECS 状态
- 🟥 创建新效果
- 🟥 覆盖已记录的 base_amount

规则3禁止：
- 🟥 创建新 CombatIntent
- 🟥 跳过死亡判定

规则4禁止：
- 🟥 match 分发效果类型
```

**原因**：宪法要求所有禁止事项标注强制等级。

---

### 8. 流程管线步骤添加强制等级（修改）

**位置**：流程管线 → 效果管线 Step1-3 / 伤害计算管线 Step1-4

**变更类型**：修改

**原内容**：无强制等级标记

**新内容**：
```
效果管线：
Step1: 🟥 禁止：修改 ECS 状态（除 EffectQueue）
Step2: 🟥 禁止：修改 ECS 状态、创建新效果
Step3: 🟥 禁止：创建新 CombatIntent

伤害计算管线：
Step1: 🟥 禁止：ignore_def 基于 effective_def（必须基于 base_def）
Step4: 🟥 禁止：跳过下限保护
```

**原因**：宪法要求所有禁止事项标注强制等级。

---

### 9. 数据结构添加强制等级（修改）

**位置**：数据结构 → EffectDef / PendingEffectData / EffectQueue / EffectHandler / GenerateContext

**变更类型**：修改

**新增内容**：
```
PendingEffectData: 🟥 base_amount 在 Modify 首次记录
EffectQueue: 🟥 Generate 推入，Modify 修改，Execute drain 清空、🟥 Execute 后 pending 必须为空、🟩 不可当全局变量仓库（宪法 2.1.5）
EffectHandler: 🟩 新增效果类型只需实现并注册（宪法 6.0.2）
GenerateContext: 🟩 纯数据快照（宪法 2.1.2）、🟩 Entity 仅作为 ID 传递（宪法 2.1.1）
```

**原因**：宪法要求所有规则标注强制等级和宪法条款引用。

---

### 10. 禁止事项添加强制等级和新增1条（修改+新增）

**位置**：禁止事项

**变更类型**：修改 + 新增

**修改内容**：原有5条禁止事项全部添加🟥标记和宪法引用

**新增内容**：
```
🟥 禁止：绕过 Modifier 管线直接修改属性
原因：宪法 2.2.1 禁止直接修改最终属性值
架构违规检测：ARCHITECTURE VIOLATION: 直接修改最终属性值...
```

**原因**：宪法 2.2.1 明确要求。

---

### 11. AI 修改规则添加强制等级和测试要求（修改）

**位置**：AI 修改规则

**变更类型**：修改

**新增内容**：
```
禁止：
- 🟥 match 分发效果类型
- 🟥 修改管线三步流程
- 🟥 修改下限保护规则

优先检查：
- 🟥 伤害下限 ≥ 1
- 🟥 ignore_def_percent 基于 base_def

测试要求（宪法 13.0.1-13.0.3）：
- 🟩 单元测试：验证管线各阶段输入输出
- 🟩 集成测试：验证完整效果管线
- 🟩 Bug 修复必须先编写重现测试（宪法 13.0.2）
- 🟩 Battle Replay 优先于手工验证（宪法 1.1.9）
```

**原因**：宪法要求所有禁止事项标注强制等级；宪法 13.0.1-13.0.3 要求测试优先。

---

### 12. 宪法条款映射（新增）

**位置**：文档末尾

**变更类型**：新增

**原因**：便于 AI 快速定位宪法条款与领域规则的对应关系。

---

### 13. 架构违规检测（新增）

**位置**：文档末尾

**变更类型**：新增

**原因**：架构文档定义了标准违规检测模式，领域文档应纳入。

---

## 变更统计

| 强制等级 | 原文档数量 | 新文档数量 |
|----------|-----------|-----------|
| 🟥 绝对禁止 | 0 | 14 |
| 🟩 必须遵守 | 0 | 8 |
| 宪法条款引用 | 0 | 10 |
| 架构违规检测 | 0 | 2 |
