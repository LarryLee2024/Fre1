# ui_rules.md → ui_rules_v1.md 更新差异

Version: 1.0 → 1.1

---

## 变更总览

| 变更类型 | 数量 |
|----------|------|
| 新增 | 4 |
| 修改 | 9 |
| 删除 | 0 |

---

## 逐项变更

### 1. 核心原则（修改）

**位置**：核心原则

**变更类型**：修改

**原内容**：
```
- UI 不操作 ECS，只发出意图
- ViewModel 层隔离
- UI 监听状态变化刷新自己
- 主题系统统一样式
- 焦点管理
```

**新内容**：
```
- 🟥 UI 不操作 ECS，只发出意图（宪法 1.1.4 逻辑与表现分离）
- 🟥 ViewModel 层隔离（宪法 1.1.4）
- 🟩 UI 监听状态变化刷新自己
- 🟩 主题系统统一样式
- 🟩 焦点管理
```

**原因**：宪法要求所有规则标注强制等级；补充宪法条款引用。UI 领域是宪法 1.1.4 的核心体现。

---

### 2. 不变量1-4 添加强制等级和宪法引用（修改）

**位置**：不变量1-4

**变更类型**：修改

**原内容**：无强制等级标记，无宪法引用

**新内容**：
```
不变量1：UI 不操作 ECS 🟥
宪法依据：1.1.4（逻辑与表现分离）、2.2.2（禁止业务逻辑直接操作 UI）

不变量2：ViewModel 隔离 🟥
宪法依据：1.1.4（逻辑与表现分离）、2.2.2（禁止 UI 保存业务真相）

不变量3：handle_ui_commands 仅玩家回合 🟥
不变量4：BlocksGameInput 阻止输入 🟥
```

**原因**：宪法要求所有规则标注强制等级和宪法条款引用。

---

### 3. 不变量1-2 新增架构违规检测（修改）

**位置**：不变量1-2

**变更类型**：修改

**新增内容**：
```
不变量1架构违规检测：
ARCHITECTURE VIOLATION: UI 直接修改 ECS 状态，违反"逻辑与表现分离"原则。

不变量2架构违规检测：
ARCHITECTURE VIOLATION: UI 直接 Query 游戏组件，违反"ViewModel 隔离"原则。
```

**原因**：UI 直接修改 ECS 和直接 Query 是核心架构违规。

---

### 4. 不变量5：UI 不保存业务真相（新增）

**位置**：不变量

**变更类型**：新增

**新内容**：
```
## 不变量5：UI 不保存业务真相 🟥

宪法依据：2.2.2（禁止 UI 保存业务真相或修改业务状态）

任意时刻：
UI 不缓存或保存业务状态的副本，ViewModel 是只读投影。

违反表现：
UI 组件缓存 HP 值并在本地计算伤害，而非从 ViewModel 读取。

架构违规检测：
ARCHITECTURE VIOLATION: UI 保存业务真相，违反"UI 不保存业务真相"原则。
```

**原因**：宪法 2.2.2 明确禁止 UI 保存业务真相，这是 UI 领域的核心不变量。

---

### 5. 规则1-4 添加强制等级和宪法引用（修改）

**位置**：业务规则1-4

**变更类型**：修改

**原内容**：无强制等级标记，无宪法引用

**新内容**：
```
规则1：UiCommand 是唯一交互通道 🟥
宪法依据：1.1.4（逻辑与表现分离）、5.0（通信三原则）

规则2：ViewModel 刷新策略 🟩
规则3：Cancel 上下文推断 🟩
规则4：主题统一样式 🟩
```

**原因**：宪法要求所有规则标注强制等级和宪法条款引用。

---

### 6. 规则1-4 禁止事项添加强制等级（修改）

**位置**：规则1-4

**变更类型**：修改

**原内容**：无强制等级标记

**新内容**：
```
规则1禁止：
- 🟥 UI 直接修改 ECS 状态
- 🟥 UI 直接调用游戏逻辑函数

规则2禁止：
- 🟥 每帧重建 ViewModel

规则4禁止：
- 🟥 硬编码颜色/字号
- 🟥 绕过 UiTheme 直接写样式
```

**原因**：宪法要求所有禁止事项标注强制等级。

---

### 7. 流程管线步骤添加强制等级（修改）

**位置**：流程管线 → UI 命令处理管线 Step1-3 / ViewModel 更新管线 Step1-2

**变更类型**：修改

**原内容**：无强制等级标记

**新内容**：
```
UI 命令处理管线：
Step1: 🟥 禁止：AI 回合执行 UI 命令
Step2: 🟥 禁止：UI 直接修改 ECS
Step3: 🟩 允许：跳过范围标记（非关键路径）

ViewModel 更新管线：
Step1: 🟥 禁止：每帧重建
Step2: 🟥 禁止：UI 直接 Query
```

**原因**：宪法要求所有禁止事项标注强制等级。

---

### 8. 数据结构添加强制等级（修改）

**位置**：数据结构 → UiCommand / SelectedUnitView / UiTheme / UiFocusState

**变更类型**：修改

**新增内容**：
```
UiCommand: 🟥 每个命令有明确的阶段映射
UiTheme: 🟥 所有 UI 组件从 UiTheme 读取样式
UiFocusState: 🟥 BlocksGameInput 组件标记模态面板
```

**原因**：宪法要求所有规则标注强制等级。

---

### 9. 禁止事项添加强制等级和宪法引用（修改）

**位置**：禁止事项

**变更类型**：修改

**原内容**：无强制等级标记，无宪法引用

**新内容**：5条禁止事项全部添加🟥标记和宪法引用

```
🟥 禁止：UI 直接修改 ECS 状态（宪法 1.1.4）
🟥 禁止：UI 直接 Query 游戏组件（宪法 1.1.4）
🟥 禁止：硬编码颜色/字号
🟥 禁止：AI 回合执行 UI 命令
🟥 禁止：每帧重建 ViewModel
```

**原因**：宪法要求所有禁止事项标注强制等级。

---

### 10. 禁止事项新增1条（新增）

**位置**：禁止事项

**变更类型**：新增

**新内容**：
```
🟥 禁止：UI 保存业务真相
原因：宪法 2.2.2 禁止 UI 保存业务真相或修改业务状态
架构违规检测：ARCHITECTURE VIOLATION: UI 保存业务真相...
```

**原因**：宪法 2.2.2 明确要求。

---

### 11. 禁止事项新增架构违规检测（修改）

**位置**：禁止事项 → "UI 直接修改 ECS 状态" / "UI 直接 Query 游戏组件"

**变更类型**：修改

**新增内容**：
```
UI 直接修改 ECS 状态：
ARCHITECTURE VIOLATION: UI 直接修改 ECS 状态，违反"逻辑与表现分离"原则。

UI 直接 Query 游戏组件：
ARCHITECTURE VIOLATION: UI 直接 Query 游戏组件，违反"ViewModel 隔离"原则。
```

**原因**：UI 违规是核心架构违规。

---

### 12. AI 修改规则添加强制等级和测试要求（修改）

**位置**：AI 修改规则

**变更类型**：修改

**新增内容**：
```
禁止：
- 🟥 面板直接 Query 游戏组件
- 🟥 面板直接修改 ECS 状态
- 🟥 修改现有命令的处理逻辑
- 🟥 跳过阶段检查
- 🟥 绕过 UiTheme 直接写样式

测试要求（宪法 13.0.1-13.0.3）：
- 🟩 单元测试：验证 UiCommand 分发和 ViewModel 隔离
- 🟩 集成测试：验证 UI 交互流程
- 🟩 Bug 修复必须先编写重现测试（宪法 13.0.2）
```

**原因**：宪法要求所有禁止事项标注强制等级；宪法 13.0.1-13.0.3 要求测试优先。

---

### 13. 宪法条款映射（新增）

**位置**：文档末尾

**变更类型**：新增

**原因**：便于 AI 快速定位宪法条款与领域规则的对应关系。

---

### 14. 架构违规检测（新增）

**位置**：文档末尾

**变更类型**：新增

**原因**：架构文档定义了标准违规检测模式，领域文档应纳入。UI 领域有3种核心违规。

---

## 变更统计

| 强制等级 | 原文档数量 | 新文档数量 |
|----------|-----------|-----------|
| 🟥 绝对禁止 | 0 | 14 |
| 🟩 必须遵守 | 0 | 7 |
| 宪法条款引用 | 0 | 4 |
| 架构违规检测 | 0 | 3 |
