
# Bevy 0.18+ SRPG 架构宪法 v1.6 · AI协作篇
> 定位：AI生成代码的直接执行依据，精简可直接作为提示词注入
> 适用场景：AI编码、AI改Bug、AI代码评审，所有AI输出必须遵守本篇

## 效力说明
1. 本篇对所有AI生成代码具有最高执行优先级，生成前必须通读，生成后必须自检
2. 违反本篇核心规则的输出视为不合格，必须立即重写
3. 豁免规则与全文一致，所有豁免必须标注理由与有效期

---

## 一、最高优先级10条铁则
> 超越所有其他条款，任何情况下必须首先遵守
1. 🟥 **Feature First**：按业务拆模块，不按技术拆全局目录
2. 🟥 **三重分离**：配置与运行时分离、规则与内容分离、逻辑与表现分离
3. 🟥 **四级通信**：Hook生命周期、Trigger事件链、Observer局部响应、Message跨域广播
4. 🟥 **属性管线**：基础/派生属性走Modifier，资源值可直接修改
5. 🟥 **分层架构**：领域层→应用层→表现层，禁止反向依赖
6. 🟥 **领域纯函数**：核心逻辑不绑定ECS，读路径无副作用
7. 🟥 **数据驱动**：成熟点配置扩展，新机制允许改代码
8. 🟥 **确定性**：核心战斗可重现，统一RNG，禁止直接随机
9. 🟥 **测试优先**：Battle Replay + 自动化测试优先于手工验证
10. 🟥 **组合优先**：差异化通过组件/Trait/Modifier组合实现

---

## 二、AI反模式黑名单（24条）
> 生成代码前必须对照检查，违反任意一条必须立即重写
1. ❌ 把Entity当面向对象实例，模拟`player.attack(enemy)`
2. ❌ 把Resource当全局变量仓库
3. ❌ 创建全局顶层的`systems.rs/components.rs/events.rs`巨文件
4. ❌ 滥用事件/Trigger模拟普通函数调用
5. ❌ 业务逻辑直接操作UI组件、修改UI状态
6. ❌ 直接修改基础/派生属性的最终数值
7. ❌ 为单个实现创建无价值的Trait
8. ❌ 为未明确的未来需求提前设计复杂架构
9. ❌ 手写bool脏标记检测组件生命周期变化
10. ❌ Release版本每帧输出INFO/DEBUG级别日志
11. ❌ 输出技术流水账日志，而非领域事件日志
12. ❌ 业务代码直接手写`info!`输出核心业务事件
13. ❌ 使用全局AppError、anyhow::Error作为业务层统一错误类型
14. ❌ 核心业务领域代码中使用unwrap/expect/panic
15. ❌ 混淆规则失败与程序错误，将正常规则不满足作为Err返回
16. ❌ 业务代码提交todo!/unimplemented!()占位
17. ❌ 新增核心业务系统未附带任何测试用例
18. ❌ 新增领域事件未纳入白名单文档直接使用
19. ❌ 核心领域逻辑直接依赖Bevy ECS类型，不做纯函数抽象
20. ❌ 预览/仿真等读路径带有副作用
21. ❌ 业务代码直接调用原始随机数API，不使用统一RNG服务
22. ❌ 代码中硬编码数值平衡魔法数字
23. ❌ 跨模块直接修改其他Feature的内部状态
24. ❌ 未经明确授权创建新Feature、修改公共API

---

## 三、强制自检清单（生成后必须标注）
> 所有AI生成的代码文件，开头必须包含以下自检结果
```rust
// ================================================
// Bevy SRPG AI宪法 v1.6 自检结果
// ================================================
// ✅ 按业务拆分模块，无全局技术型巨文件：是
// ✅ 配置与运行时分离：是
// ✅ 逻辑与表现分离：是
// ✅ 组合实现差异化，无继承模拟：是
// ✅ 未直接操作UI与修改UI状态：是
// ✅ 基础/派生属性通过Modifier修改：是
// ✅ 日志符合领域事件规范：是
// ✅ 业务事件未直接手写info!：是
// ✅ 无违规技术流水账日志：是
// ✅ 错误分领域定义，无全局统一错误类型：是
// ✅ 核心业务层无unwrap/expect/panic/todo：是
// ✅ 区分规则失败与程序错误，语义正确：是
// ✅ 日志与错误使用强类型可读ID：是
// ✅ 无必修级Rust/Clippy警告：是
// ✅ 新增核心系统附带对应测试用例：是
// ✅ 新增领域事件已纳入白名单：是
// ✅ 核心领域逻辑纯函数化，不绑定引擎：是
// ✅ 读路径无副作用，读写分离：是
// ✅ 使用统一RNG服务，无直接随机调用：是
// ✅ 无硬编码数值魔法数字：是
// ✅ 未越权修改公共API或新增Feature：是
// ================================================
// ❌ 违反条款：X.X.X（条款编号）
// [宪法豁免] 理由：XXX
// 有效期：YYYY-MM-DD，下次架构复盘重新评估
// ================================================
```

---

## 四、AI权限边界
- 🟥 **禁止越权操作**
  - 未经明确授权，禁止创建新的Feature模块
  - 未经明确授权，禁止修改各Feature的公共API定义
  - 未经明确授权，禁止调整核心架构分层与依赖方向
- 允许范围：Feature内部的业务逻辑实现、Bug修复、测试编写、性能优化

---

## 五、Bug修复流程规范
1. 先复现：战斗Bug必须先通过Battle Replay重现
2. 先写测试：修复前必须先编写对应的重现测试用例
3. 再修复：基于规则修复，不破坏现有架构
4. 沉淀资产：修复完成后测试用例永久保留
5. 更新自检：修复后重新运行自检清单

---

## 六、质量门禁标准
AI提交的代码必须同时满足以下条件，否则视为不合格：
1. 无P0级致命问题（panic、循环、泄漏、存档损坏）
2. 无必修级Rust警告与Clippy警告
3. 新增核心功能附带至少1个基础测试
4. 代码格式符合`cargo fmt`规范
5. 自检清单完整填写，无遗漏项
6. 未越权修改架构与公共接口

---

## 七、豁免规则
- 仅P1及以下级别问题允许申请豁免
- P0级问题不可豁免，必须修复
- 所有豁免必须标注：违反条款、豁免理由、有效期
- 豁免有效期最长3个月，架构复盘时重新评估

---

## 使用建议
- 日常开发业务逻辑：优先参考 **SRPG专项篇**
- 编写ECS底层与系统调度：优先参考 **ECS篇**
- 设计模块与架构方案：优先参考 **架构篇**
- 给AI注入提示词、做代码生成：直接使用 **AI协作篇**