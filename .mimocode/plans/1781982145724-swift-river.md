# Plan: 完成 communication-debt 文档并归档

## Context

`docs/11-refactor/communication-debt-2026-06-21.md` 是唯一未归档到 `done/` 的技术债文件。文档包含 5 个阶段，部分已完成、部分需要代码修复。用户要求完成所有阶段内容、更新状态，然后移动到 `done/`。

## 当前状态分析

| 阶段 | 文档标记 | 实际状态 | 需要的动作 |
|------|---------|---------|-----------|
| 1. Event History | 未开始 | 零代码，ADR-059 已 Accepted | 更新文档：标记为 Deferred（超出本次范围） |
| 2. Trigger 审查 | 待排期 | Trigger 系统已完整实现（evaluator、TriggerType、TriggerFrequency） | 审计后标记完成 |
| 3. Observer 安全 | 待排期 | `MAX_OBSERVER_DEPTH=10` 已存在，但无深度超限日志 | 添加审计日志 |
| 4. 共享事件 | 已完成 | 仅 TurnEnded 桥接；TurnStarted/BattleStarted/BattleEnded 从未触发（dead code） | 桥接 3 个缺失事件 |
| 5. 文档对齐 | 待排期 | ECS规则§2.3 已有深度限制；宪法§6.3 缺少 Observer 深度限制 | 更新文档 |

## 实施步骤

### Step 1: 桥接 3 个缺失的共享事件（阶段 4 补完）

**文件**: `src/core/domains/combat/pipeline/steps.rs`

在 `step_turn_start()` 中添加 `TurnStarted` 共享事件触发：
```rust
use crate::core::events::TurnStarted;
// 在 commands.trigger(OnTurnStart{..}) 之后添加：
commands.trigger(TurnStarted { unit: current.entity });
```

在 `step_turn_end()` 中，当 `check_team_elimination` 返回 true 时，添加 `BattleEnded` 共享事件：
```rust
use crate::core::events::{BattleEnded, BattleStarted};
// OnBattleEnd 触发后添加：
commands.trigger(BattleEnded { victory: true });
```

**文件**: `src/core/domains/combat/systems/turn_systems.rs`

在 `on_enter_battle()` 中添加 `BattleStarted` 共享事件：
```rust
use crate::core::events::BattleStarted;
// 在 commands.trigger(OnBattleStart) 之后添加：
commands.trigger(BattleStarted);
```

### Step 2: Observer 深度审计日志（阶段 3）

**文件**: `src/core/capabilities/event/mechanism/bus.rs`

在 `dispatch_pending()` 中，已有 `EVENT_CYCLE_LIMIT` 循环检测。需要确认是否有 observer depth 检查。如果 `MAX_OBSERVER_DEPTH` 未在 EventBus 中使用，在分发路径上添加深度审计日志。

**文件**: `.trae/rules/ECS规则.md` — 已有 §2.3 深度限制条款，无需更新。

### Step 3: Trigger 审计结论（阶段 2）

审计 `trigger/mechanism/evaluator.rs`：
- TriggerType 有 11 个变体 + OnCustom
- TriggerFrequency 支持 max_per_turn 频率限制
- evaluate_trigger() 是纯函数，三阶段检查（类型→频率→条件）
- TriggerContainer 支持双索引查找
- CombatTriggerFacade 封装战斗特定类型

结论：Trigger 系统实现完整，标记为完成。

### Step 4: 文档对齐（阶段 5）

**宪法 §6.3**: 补充 Observer 深度限制条款：
```
- 🟥 Observer 递归深度必须限制在 MAX_OBSERVER_DEPTH（默认 10）以内
- 🟥 禁止 Observer 触发同一事件类型的递归循环无保护
```

**ECS规则.md**: §2.3 已有深度限制，§五已有递归防护速查，无需更新。

**ADR-002 §3**: 白名单使用 struct 而非枚举 — 与实际一致，无需修改。

### Step 5: 更新 communication-debt 文档状态

更新 `communication-debt-2026-06-21.md`：
- 阶段 1: 标记为 Deferred（Event History 是新功能开发，非技术债修复）
- 阶段 2: 标记为 ✅ 完成
- 阶段 3: 标记为 ✅ 完成
- 阶段 4: 更新为 ✅ 完成（补充桥接细节）
- 阶段 5: 标记为 ✅ 完成
- 更新文档顶部状态行

### Step 6: 更新 README.md

更新 `docs/11-refactor/README.md`：
- 移除 `communication-debt-2026-06-21.md` 的待处理标记
- 标记通信系统为已完成

### Step 7: 移动到 done/

```bash
mv docs/11-refactor/communication-debt-2026-06-21.md docs/11-refactor/done/
```

## 验证

1. `cargo check` — 确认代码编译通过
2. `cargo nextest run` — 确认测试通过
3. 检查 `docs/11-refactor/` 根目录无残留文件（仅 README.md + done/）

## 关键文件

- `src/core/domains/combat/pipeline/steps.rs` — 桥接 TurnStarted, BattleEnded
- `src/core/domains/combat/systems/turn_systems.rs` — 桥接 BattleStarted
- `src/core/events.rs` — 共享事件定义（只读）
- `docs/00-governance/ai-constitution-complete.md` — §6.3 更新
- `docs/11-refactor/communication-debt-2026-06-21.md` — 状态更新
- `docs/11-refactor/README.md` — 状态更新
